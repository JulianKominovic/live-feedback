import { motion } from "framer-motion";
import styled from "@emotion/styled";
export const VerticalDivider = styled(motion.hr)`
  width: 1px;
  height: 80%;
  border: none;
  margin: 0;
  background-color: rgba(0, 0, 0, 0.2);
`;
